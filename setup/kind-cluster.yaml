# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: test
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 31380
        hostPort: 31380
        protocol: TCP
      - containerPort: 30080  # <-- HTTP (Istio Gateway)
        hostPort: 30080
        protocol: TCP
