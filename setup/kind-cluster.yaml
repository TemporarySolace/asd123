# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: test
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 80
        hostPort: 3180
        listenAddress: "0.0.0.0"
        protocol: TCP
